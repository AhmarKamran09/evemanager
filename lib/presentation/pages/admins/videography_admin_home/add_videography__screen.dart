import 'package:flutter/material.dart';

class AddVideographyScreen extends StatelessWidget {
  const AddVideographyScreen({Key? key, required this.uid});
  final String uid;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
